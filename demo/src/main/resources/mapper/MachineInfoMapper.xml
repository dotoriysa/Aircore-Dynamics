<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.MachineInfoMapper">

    <select id="getAllPmIds" resultType="java.lang.String">
        SELECT pm_id
        FROM process_machine_info
    </select>

</mapper>