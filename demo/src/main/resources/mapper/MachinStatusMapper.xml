<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.MachineStatusMapper">

    <!-- 장비 상태 데이터 삽입 -->
    <insert id="insertMachineStatusData" parameterType="com.example.demo.model.MachineStatusData">
        <![CDATA[
        INSERT INTO machine_status_data (
            timestamp,
            pm_id,
            status
        ) VALUES (
            #{timestamp},
            #{pmId},
            #{status}
        )
        ]]>
    </insert>

    <select id="selectLatestMachineStatus" resultType="com.example.demo.model.MachineStatusData">
        <![CDATA[
        SELECT
            id,
            timestamp,
            pm_id,
            status
        FROM
            machine_status_data
        WHERE
            pm_id = #{pmId}
        ORDER BY
            timestamp DESC
        LIMIT 1
        ]]>
    </select>

    <select id="selectAllMachineStatusRecords" resultType="com.example.demo.model.MachineStatusData">
        <![CDATA[
    SELECT
        timestamp,
        status
    FROM
        machine_status_data
    WHERE
        pm_id = #{pmId}
        AND DATE(timestamp) = CURDATE()
    ORDER BY
        timestamp ASC
    ]]>
    </select>

</mapper>